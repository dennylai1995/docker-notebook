version: '3.8'
services:    
  web_bridge:
    image: busybox
    tty: true # Allocates a pseudo-TTY to keep a container running.
    network_mode: bridge
    
  db_bridge:
    image: busybox
    tty: true
    ports:
    - 5000:5001
    network_mode: bridge
